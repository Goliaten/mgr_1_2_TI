@model Movie

<div>
    <table>
        <tr>
            <td>
                <h1>@Model.Category.Name</h1>
                @(await Component.InvokeAsync("SideMenu", new { Model.CategoryId }))
            </td>
            <td>
                <ul>
                    <li>@Model.Title</li>
                    <li>@Model.Director</li>
                    <li>@Model.Description</li>
                    <li>@Model.Price zł</li>
                </ul>
            </td>
        </tr>
    </table>
</div>